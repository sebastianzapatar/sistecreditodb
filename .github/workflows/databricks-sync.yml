name: Sync to Databricks on Push

on:
  push:
    branches: [ main ]

jobs:
  sync-databricks:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Update Databricks Repo
      run: |
        echo "🔄 Updating sistecreditodb repo..."
        
        # Usar el ID exacto del repo
        REPO_ID="4472650222095333"
        
        echo "📍 Using repo ID: $REPO_ID"
        echo "🌐 Host: ${{ secrets.DATABRICKS_HOST }}"
        
        # Actualizar repo con el hostname correcto
        curl -X PATCH \
          "${{ secrets.DATABRICKS_HOST }}/api/2.0/repos/$REPO_ID" \
          -H "Authorization: Bearer ${{ secrets.DATABRICKS_TOKEN }}" \
          -H "Content-Type: application/json" \
          -d '{"branch": "main"}' \
          --fail-with-body \
          -v
        
        echo "✅ Databricks repo synced successfully!"
